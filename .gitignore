# ---- Временные / системные ----
Thumbs.db
.DS_Store
desktop.ini
~$*

# ---- Power BI кеши ----
*.pbix.tmp
*.tmp

# ---- Крупные сырые данные (в репо только сэмпл + ссылка на источник) ----
**/data/online_retail_II*.csv
**/data/Fact_Sales.csv          # полный факт (~89 МБ) — не коммитим, только Fact_Sales_sample.csv

# ---- Личное / черновики ----
/private/
/drafts/
*.docx
*.xlsx

# =====================================================================
#  ЗАЩИТА ОТ УТЕЧКИ РАБОЧИХ ДАННЫХ — НЕ УДАЛЯТЬ
#  Ни один скриншот/файл с рабочими данными не должен попасть в публичный репозиторий.
#  Разрешаем в images/ только явно названные кадры e-commerce проекта.
# =====================================================================
**/screenshots/
image copy*.png
image.png
